var int windowWidth = 800;
var int windowHeight = 600;

var int posX = windowWidth / 2;
var int posY = windowHeight /2;

cursor c1 = create_cursor(posX, posY);
c1.color(TEAL);
c1.draw_ellipse(300, 150,true);
